Render Build Log Analysis - Mar 4, 2026

1. db:push ran but hit ER_TABLE_EXISTS_ERROR for the `users` table
   - This is because drizzle-kit generate creates migration SQL for ALL tables, including existing ones
   - The migration failed with exit code 1
   - BUT the build continued anyway (pnpm install; pnpm db:push; pnpm run build - the semicolons don't stop on failure)

2. The build itself succeeded: "Build successful"
3. Deploy is "In Progress"

Key issue: The migration tries to CREATE TABLE `users` which already exists.
The health tables (health_upload_jobs, health_buckets, health_workouts) may or may not have been created 
depending on migration order.

Fix needed: Use && instead of ; in the build command so failures stop the build,
OR better: make the migration idempotent (IF NOT EXISTS), 
OR: the drizzle-kit migrate command should handle this - need to check if the __drizzle_migrations table 
tracks which migrations have been applied.

Actually wait - the issue is that the migration journal on production may be out of sync.
The health table migration (0003) may have been created AFTER the users table migration was already applied.
So drizzle-kit is trying to re-apply old migrations.

Need to investigate the __drizzle_migrations table on production.
